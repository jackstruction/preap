import React from 'react';

const NumLine = (props) => {
    return (
        <>
            <div id={props.id}>{props.placeholder}</div>
               
        </>
    );
    }

    export default NumLine;